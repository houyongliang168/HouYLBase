ext {

    isModuleRun = [
            login : false,//默认为 lib 运行
            cusotm: false

    ]

    isDebug = [
            login : true,//默认debug
            cusotm: false
    ]
    // 域名配置
    url = [
            "debug"  : "debugUrl",
            "release": "releaseUrl"
    ]


    isLoginModuleRun = [//判断是否 依赖 登陆运行
                        custom: false,//默认为 lib 运行
                        other : false

    ]

}








